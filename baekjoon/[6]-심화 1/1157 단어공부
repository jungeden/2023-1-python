
a=input()
a=a.upper()
A=[]
B=[]
for i in a:
    B.append(i)
    A.append(a.count(i))
b=max(A)
if A.count(max(A)) > 2: #처음엔 리스트를 2개 만들어서 복제 한 리스트에서 가장 큰 수를 삭제하고 원래 리스트에서 가장 큰 값을 복제한 리스트랑 비교해서 값이 같으면 가장 큰 값이 2개.
    print('?')          #그래서 '?' 하고 아니면 인덱스로 넘어가게 함. b가 C에 있으면 같은 값 두 개
else:                   #근데 리스트가 연동되 듯 한 리스트의 요소 삭제하면 다른 리스트 값도 똑같이 삭제됨. 그래서 계속 ?뜨고 무한반복 함.
    c=A.index(max(A))   #그래서 리스트 말고 count 써서 그 값이 2개 이상이면 '?' 표시 함. 아닌 경우 else. 이렇게 하니까 됨!
    print(B[c])